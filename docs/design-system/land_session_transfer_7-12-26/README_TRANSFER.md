# LAND PRODUCT SESSION TRANSFER — 2026-07-11
**From:** Claude.ai chat project (strategy side), session VP
**To:** Cowork / Landscape project (coding side)
**Purpose:** Full context transfer for the standalone land development product direction.

## Read in this order
0. `HANDOFF_NARRATIVE.md` — the genesis story: why this direction exists, prototype by prototype. Read for context; the briefing governs where they differ.
1. `CHAT_TO_CW_SYNC.md` — the sync-bridge file (seq 1). Current focus, locked decisions, what's requested from the coding side. **This and #2 also belong in the designated sync folder**, not just here.
2. `LANDSCAPE_LAND_SLICE1_BRIEFING.md` — the operative document: locked register D1–D8, slice 1 scope, in/out lists, human gates, and the hard precondition (read-only discovery audit of map/overlay/parcel machinery BEFORE any phasing proposal).
3. `landscape_land_locked_register.html` — Gregg's plain-English copy of the same register.
4. `landscape_land_workflow_spine.html` — the six-stage product spine with prototype traceability (pre-lock version; the register supersedes it where they differ).

## Interaction evidence (disposable prototypes — patterns, not code)
- `ingestion_prototype_1..4` — intake paradigm exploration: conversational live-fill (1), batch briefing with no chat (2), decision tiles with in-tile Q&A (3, Gregg's own idea — the Q&A can surface options the original menu lacked), decision docket at scale with blockers vs. defaults (4).
- `ingestion_prototype_5..5i` — MPC intake from the Peoria 1600 exhibit: parcel program from a map graphic, Area→Phase→Parcel decode with user confirmation (5b), grouped/flat parcel table toggle (5c/5i, flat column order Area→Phase→Parcel shows ID construction), layout densification iterations (5d–5h, treemap + side legend won).
- `ingestion_prototype_6_red_valley_ranch.html` — intake from the REAL Red Valley Ranch entitlement record: precedence chain (recorded > approved > proposed), entitlement clocks as risk items, honest scanned-file flagging. Parcel data verbatim from the pre-plat narrative.
- `prototype_7` — map-driven navigation: parcel/phase/area selection modes, rollups, takedown tiles bidirectionally linked to the plan.
- `prototype_8` — spatial parcel-table authoring: multi-select + paint modes, cascading family→type→product form (stand-in for the canonical land use library).
- `prototype_9` — street spec derivation from plan sets without CAD: stated/measured/derived/assumed provenance tags, reconciliation loop vs. engineer tabs. (v1.5 material per D5.)
- `prototype_10`, `10b` — streets as first-class clickable objects with standards-library defaults and per-segment quantities; class-based cartographic symbology.
- `engineering_deliverables_request.html` — the machine-readable formats to request from civil engineers (LandXML first), feeds D4's CAD/GIS path.

## Key session facts the coding side should know
- All 8 decisions locked by Gregg on 2026-07-11; recorded in the briefing. Do not re-litigate.
- Pilot = Red Valley Ranch. Its entitlement documents live in the OneDrive project folders (PAD narrative, pre-plat narrative, SUB21-20, PPE23-01, SUB24-09 final plat). Note: the preliminary plat staff report and several plat PDFs have no readable text layer through the chat-side connector — the platform pipeline (OCR/vector) is the read path.
- The chat side's outbound sync counter starts at seq 1 with this package. The coding side's own outbound file was last seen at seq 1 / 2026-06-16 from the chat side; if it has been updated since, the cloud copy is not syncing — verify the write location and the folder-sync service.
