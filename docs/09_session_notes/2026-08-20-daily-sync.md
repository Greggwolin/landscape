# Daily Sync — 2026-08-20 (Wednesday)

**Generated by:** nightly-landscape-sync (automated)
**Branch:** main

---

## Summary

Quiet day — no commits landed. Uncommitted work on `main` from an in-progress session covers two tracks: a test-database safety guard and a pending-migrations consolidation.

## Uncommitted Changes on `main`

### Track 1: Test-Database Safety Guard
- **`backend/conftest.py`** (+92 lines) — new `_point_tests_at_a_disposable_database` fixture that refuses to let `pytest-django` create scratch databases on a remote server (e.g. production Neon) unless an explicit env var override is set. Classifies hosts as disposable (localhost, loopback, unix socket) vs. remote.
- **`backend/.env.example`** (+12 lines) — documents `TEST_DATABASE_URL` and the safety guard rationale; notes that `test_land_v2` and `test_test_land_v2` were found sitting in the live Neon project as leftovers of interrupted runs.
- **`backend/apps/projects/test_db_guard.py`** (new, untracked) — regression tests for the disposable-host classifier.

### Track 2: Pending Migrations Consolidation
- **`migrations/20260820_apply_pending_2026.up.sql`** (new, untracked) — consolidates 2026 schema changes verified absent against the live Neon database; dry-run validated inside a rolled-back transaction.
- **`migrations/20260820_apply_pending_2026.down.sql`** (new, untracked) — rollback companion.

### Track 3: Nightly Audit
- **`CLAUDE.md`** (3 lines changed) — last night's nightly sync promoted the 08-19 audit entry to "Last audit" position; prior 08-18 entry demoted to "Prior audit".

## Categorized Changes

| Category | Items |
|----------|-------|
| Features | — |
| Bug fixes | — |
| Tech debt | Test-database safety guard (prevents accidental scratch DB creation on production Neon) |
| Infrastructure | Pending-migrations consolidation (2026 schema changes not yet applied to live DB) |
| Docs | CLAUDE.md audit rotation |
| Issues found | `test_land_v2` and `test_test_land_v2` found in live Neon project (leftovers of interrupted pytest runs) |

## Carry-Forward Items

- Demo project re-clone; verify the floor-plan double-counting fix deployed
- Plan geometry pipeline — merged to main (PR #247) but not integration-tested against a real uploaded plat; no Landscaper tools registered yet
- 176 tables have no migration or DDL — documented in schema recoverability audit
- Slice 1 still unmerged (~20 days); dedup fix needs blast radius traced
- Uncommitted test-database guard + migration consolidation need to be committed or branched

## Tool Count

**285 registered / 282 advertised** (unchanged)
