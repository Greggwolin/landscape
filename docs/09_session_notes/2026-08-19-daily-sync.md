# Daily Sync — 2026-08-19

Auto-generated by nightly-landscape-sync.

## Git Activity (21 commits)

### Features (3)
- `d6b427d1` — **SQL file recovery (PR #251):** 290 database files recovered from blanket `*.sql` gitignore rule. `.gitignore` rewritten to exempt `migrations/` and `backend/migrations/` permanently. Adds `archive/docs/` (10 files), `archive/migrations-legacy/` (31 files), `backend/db/migrations/` (17 files), `backend/migrations/` (29 files), `migrations/` (82 files), `scripts/` SQL (15 files), `docs/05-database/sql/` (6 files), `src/app/api/db-migrations/` (13 files), `tests/fixtures/` (3 files), and other scattered `.sql` files.
- `4acd5032` — **Plan geometry GP14 merge (PR #247):** Full plan geometry extraction pipeline merged to main — `backend/apps/knowledge/services/plan_geometry/` (12 modules, ~4,000+ lines), `gis_plan_lot` table, `PlanStageCard` + `PlanPreviewWindow` frontend, `import_plan_lots` management command, 8 test files.
- `104430cd` — **Daily brief "Automated Checks" section:** +188 lines in `generate_daily_brief.py` — new section surfaces CI gate status (lint, typecheck, backend tests, gitignore SQL guard) in the nightly HTML brief.

### CI / Infrastructure (9)
- `5c296df6` + `ca334361` — **BC5 lint break/revert:** Deliberate lint failure injected and reverted to prove the CI lint gate catches violations.
- `0d076117` + `565987b7` — **BC5 typecheck break/revert:** Deliberate type error injected and reverted to prove the CI typecheck gate catches violations.
- `75767483` + `fbd9d897` — **BC5 backend break/revert + SQL guard fix:** Deliberate pytest failure injected and reverted; SQL guard structural gap fixed (+26 lines net).
- `37e97b87` + `5455f3b8` — **BC5 gitignore break/revert:** Deliberate gitignore violation injected and reverted to prove the SQL-file guard catches regressions.
- `37047065` — **Artifact-integration skip guard + disaster-drill timeout:** `test_artifact_integration.py` skip guard for missing env; disaster-drill YAML timeout fix.
- `156cf6f7` — **Disaster-drill YAML fix + jest suite wiring:** 8 files changed (+64/-24); every real jest suite wired into CI.
- `b540446e` — **CI timeout/retry fix:** Workflow reliability improvements.

### Docs (5)
- `fb4ef49c` — **Check inventory audit:** `docs/audits/CHECK-INVENTORY-2026-08-19.md` (+155 lines) — catalogues every automated check in the repo (lint, typecheck, pytest, jest, gitignore SQL guard, disaster drill, nightly committer).
- `342a6d0c` — **Schema recoverability audit:** `docs/audits/SCHEMA-RECOVERABILITY-2026-08-19.md` (+518 lines) — proves 176 tables have no migration or DDL file anywhere in the recovered corpus, documenting the gap and recoverability posture.
- `dc005096` — **Plan geometry reference doc:** `docs/02-features/plan-geometry.md` (+322 lines).
- `f21ed866` — **Nightly health check doc.**
- CLAUDE.md audit line updated to 2026-08-19.

### Chore (4)
- Merge commits and worktree cleanup (`.claude/worktrees/` pruned, `start-next.sh` updated).

## CLAUDE.md Updates
- Last audit line: 2026-08-19 reflecting SQL recovery (PR #251), BC5 Phase 2 CI proof, daily brief Automated Checks section, audit docs (check inventory + schema recoverability), plan geometry GP14 merge (PR #247), CI hardening.
- Prior audit (2026-08-18) pushed down.
- Line 368 context on gitignore/SQL recovery already present from prior session.

## Tool Count
Unchanged: 285 registered / 282 advertised.
