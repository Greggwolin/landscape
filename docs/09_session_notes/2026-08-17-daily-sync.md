# Daily Sync — 2026-08-17 (Sunday)

> Auto-generated by nightly-landscape-sync scheduled task.

---

## Git Activity (13 commits)

### Features (5)
- `ba46b9f8` **feat(map): draggable derived location point, comparables counts, panel widths (MK24)** — 12 files, +376/-30. New migration `20260817_project_location_source`. Derived location pin is now draggable; comparable counts shown in layer panel; panel width adjustments.
- `6cf744fa` **feat(map): legend for Comparable Unit Sales price tiers** — Price-tier color legend added to the map for unit sale comparables.
- `db3000e0` **feat(map): draggable layer rail, 10% wider** — Layer rail panel is now draggable and 10% wider.
- `10e2d053` **feat(knowledge): plan-geometry chain end-to-end (MK33)** — 8 files, +1030/-8. New `lot_table.py` + `plan_reader.py` + tests. The plan geometry extraction pipeline now runs end-to-end from PDF to lot table.
- `b64f28da` **feat(knowledge): Confirm reads drawing into project (MK34)** — 8 files, +807/-2. New `apply_plan.py` + `plan_apply_views.py` + tests. User confirmation step that reads an extracted drawing into the project's lot inventory.

### Bug Fixes (5)
- `fd52065c` **fix(map): chat never displaced by destination; Location + Market groups (MK28)** — 10 files, +420/-201. Chat panel no longer gets displaced when navigating to a map destination. Location and Market layer groups reorganised.
- `625a6dda` **fix(map): zero counts filtered before panel saw them (MK28 follow-up)** — Filters out zero-count comparables before the layer panel renders them.
- `70dae63e` **fix(map): /w/ shell was calling every project an income property** — Property-type detection in the `/w/` shell corrected; previously all projects were treated as income property regardless of actual type.
- `91be7ccc` **fix(nav): sidebar Map icon stops hijacking to last-visited project** — Map sidebar icon no longer forces navigation to a previously visited project.
- `715bc255` **style(map): 70% restored, layer groups styling** — Partial style restoration after MK28 refactor; layer group visual consistency.

### Style / Polish (4)
- `af8aa59a` **style(map): filters three-across, legend onto map** — Filter controls arranged three-across; price legend moved onto the map canvas.
- `74381e1d` **style(map): filter boxes sized to labels, indented under layer** — Filter checkboxes sized to their label text; indented under their parent layer toggle.
- `2ac2d1ec` **style(map): radius carries unit in box, not label** — Radius filter now shows the unit (mi) inside the input box rather than as a separate label.
- `db3000e0` **feat(map): draggable layer rail, 10% wider** — (Also style — the rail is wider and draggable.)

## Uncommitted Changes (6 files, +211/-27)
- `CLAUDE.md` — 27 lines changed (nightly audit note updates)
- `backend/apps/knowledge/views/plan_apply_views.py` — 20 lines (plan apply endpoint refinements)
- `docs/PROJECT_INSTRUCTIONS_CHANGELOG.md` — 38 lines (changelog entries)
- `src/components/preview/DocumentPreviewModal.tsx` — 30 lines (document preview updates)
- `src/components/wrapper/ProjectArtifactsPanel.tsx` — 29 lines (artifacts panel changes)
- `src/components/wrapper/documents/PlanStageCard.tsx` — 94 lines (plan stage card refinements)

## New Migration
- `20260817_project_location_source` — adds location-source provenance to project records (supporting the draggable derived-location feature in MK24).

## Architecture Notes
- **Plan geometry pipeline now runs end-to-end (MK33 + MK34).** The chain from uploaded PDF → classification → lot extraction → lot table → user confirmation → project lot inventory is complete in code. Two new modules: `lot_table.py` (structured lot table from extracted geometry) and `plan_reader.py` (reads plan PDFs). `apply_plan.py` + `plan_apply_views.py` add the confirmation step that writes extracted lots into the project. Still on `feature/plan-geometry` branch; not yet integration-tested on a real plat.
- **Map layer panel overhaul (MK24/MK28).** Significant refactor of the map's layer panel: draggable rail, Location + Market layer groups, comparable counts in panel, price-tier legends, filter controls three-across with indented checkboxes. The `/w/` shell property-type detection bug (treating all projects as income property) was also fixed.
- **New DB column: `project_location_source`.** Migration `20260817_project_location_source` adds provenance tracking for how a project's location was determined (geocoded, manual pin, derived). The derived-location pin is now draggable.

## Carry-forward Items
- Re-run demo project clones on host.
- Verify PropertyTab.tsx floor plan double-counting fix deployed.
- Plan geometry pipeline integration test on real plat upload.
- Budget artifact slice 1 still unmerged (dedup blast-radius trace pending).
