diff --git a/node_modules/xlsx/xlsx.js b/node_modules/xlsx/xlsx.js
index e61c7cc..cb8e3db 100644
--- a/node_modules/xlsx/xlsx.js
+++ b/node_modules/xlsx/xlsx.js
@@ -23930,11 +23930,14 @@ function writeFileAsync(filename, wb, opts, cb) {
 	var _cb = cb; if(!(_cb instanceof Function)) _cb = (opts);
 	return _fs.writeFile(filename, writeSync(wb, o), _cb);
 }
+function is_unsafe_header_key(key) {
+	return key === "__proto__" || key === "constructor" || key === "prototype";
+}
 function make_json_row(sheet, r, R, cols, header, hdr, dense, o) {
 	var rr = encode_row(R);
 	var defval = o.defval, raw = o.raw || !Object.prototype.hasOwnProperty.call(o, "raw");
 	var isempty = true;
-	var row = (header === 1) ? [] : {};
+	var row = (header === 1) ? [] : Object.create(null);
 	if(header !== 1) {
 		if(Object.defineProperty) try { Object.defineProperty(row, '__rowNum__', {value:R, enumerable:false}); } catch(e) { row.__rowNum__ = R; }
 		else row.__rowNum__ = R;
@@ -23954,6 +23957,10 @@ function make_json_row(sheet, r, R, cols, header, hdr, dense, o) {
 			default: throw new Error('unrecognized type ' + val.t);
 		}
 		if(hdr[C] != null) {
+			if(header !== 1 && is_unsafe_header_key(hdr[C])) {
+				if(v != null) isempty = false;
+				continue;
+			}
 			if(v == null) {
 				if(val.t == "e" && v === null) row[hdr[C]] = null;
 				else if(defval !== undefined) row[hdr[C]] = defval;
