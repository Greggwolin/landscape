'use client';

/**
 * GuideSection — re-export convenience.
 * Section rendering is handled inline by GuideContent.
 * This file exists to match the architecture spec.
 */
export { default } from './GuideContent';
